.status-bar {
  box-sizing: border-box;
  display: flex;
  align-items: center;
  justify-content: space-between;
  min-height: 88rpx;
  padding: 12rpx 4rpx;
  background: transparent;
  border: 0;
  border-radius: 12rpx;
}

.status-main {
  display: flex;
  align-items: center;
  min-width: 0;
  flex: 1;
}

.status-dot {
  width: 12rpx;
  height: 12rpx;
  flex-shrink: 0;
  border-radius: 50%;
  margin-right: 12rpx;
  background: var(--muted-ink);
}

.tone-good .status-dot {
  background: var(--green-ink);
}

.tone-busy .status-dot {
  background: var(--green-ink);
  animation: status-pulse 1.2s ease-in-out infinite;
}

.tone-warn .status-dot {
  background: var(--warning);
}

.tone-bad .status-dot {
  background: var(--danger);
}

@keyframes status-pulse {
  0%, 100% { opacity: 1; }
  50% { opacity: 0.35; }
}

.status-label {
  flex-shrink: 0;
  color: var(--ink);
  font-size: 24rpx;
  font-weight: 700;
}

.tone-warn .status-label {
  color: var(--warning);
}

.tone-bad .status-label {
  color: var(--danger);
}

.status-meta {
  margin-left: 14rpx;
  overflow: hidden;
  color: var(--muted-ink);
  font-size: 22rpx;
  text-overflow: ellipsis;
  white-space: nowrap;
}

/* Same recipe as .tool-btn-labeled — refresh and share read as one family */
.status-refresh {
  flex-shrink: 0;
  min-height: 64rpx;
  line-height: 64rpx;
  margin-left: 16rpx;
  padding: 0 22rpx;
  box-sizing: border-box;
  border-radius: 12rpx;
  background: var(--track);
  color: var(--ink);
  font-size: 22rpx;
  font-weight: 800;
  text-align: center;
}

.status-refresh-hover {
  background: var(--accent-tint);
}

.status-bar-compact {
  min-height: 64rpx;
  padding: 8rpx 4rpx;
  border: 0;
  background: transparent;
}

.status-bar-compact .status-label {
  font-size: 20rpx;
  font-weight: 600;
}

.status-bar-compact .status-meta {
  font-size: 20rpx;
}
