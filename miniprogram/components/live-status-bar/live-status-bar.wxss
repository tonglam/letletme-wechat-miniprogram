.status-bar {
  box-sizing: border-box;
  display: flex;
  align-items: center;
  justify-content: space-between;
  min-height: 88rpx;
  padding: 12rpx 20rpx;
  background: var(--card, #ffffff);
  border: 1rpx solid var(--line, #ddd4de);
  border-radius: 12rpx;
}

.status-main {
  display: flex;
  align-items: center;
  min-width: 0;
  flex: 1;
}

.status-dot {
  width: 12rpx;
  height: 12rpx;
  flex-shrink: 0;
  border-radius: 50%;
  margin-right: 12rpx;
  background: var(--muted-ink, #6e5a72);
}

.tone-good .status-dot {
  background: var(--green-ink, #008545);
}

.tone-busy .status-dot {
  background: var(--green-ink, #008545);
  animation: status-pulse 1.2s ease-in-out infinite;
}

.tone-warn .status-dot {
  background: #b25e09;
}

.tone-bad .status-dot {
  background: var(--danger, #c9183f);
}

@keyframes status-pulse {
  0%, 100% { opacity: 1; }
  50% { opacity: 0.35; }
}

.status-label {
  flex-shrink: 0;
  color: var(--ink, #301333);
  font-size: 24rpx;
  font-weight: 700;
}

.tone-warn .status-label {
  color: #b25e09;
}

.tone-bad .status-label {
  color: var(--danger, #c9183f);
}

.status-meta {
  margin-left: 14rpx;
  overflow: hidden;
  color: var(--muted-ink, #6e5a72);
  font-size: 22rpx;
  text-overflow: ellipsis;
  white-space: nowrap;
}

.status-refresh {
  flex-shrink: 0;
  min-width: 88rpx;
  height: 56rpx;
  line-height: 56rpx;
  margin-left: 16rpx;
  padding: 0 20rpx;
  box-sizing: border-box;
  border-radius: 999rpx;
  background: var(--track, #f1efe9);
  color: var(--green-ink, #008545);
  font-size: 24rpx;
  font-weight: 700;
  text-align: center;
}

.status-refresh-hover {
  background: var(--accent-tint, #dcf6ea);
}

.status-bar-compact {
  min-height: 72rpx;
  padding: 8rpx 16rpx;
}
