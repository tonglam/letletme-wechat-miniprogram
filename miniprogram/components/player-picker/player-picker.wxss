.option {
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding: 20rpx 0;
  border-bottom: 1rpx solid var(--line);
  font-size: 26rpx;
}

.muted {
  color: var(--muted-ink);
  font-size: 22rpx;
}
