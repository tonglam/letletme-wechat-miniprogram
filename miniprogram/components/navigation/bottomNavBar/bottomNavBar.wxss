/* bottomNavBar — dark fascia tab bar (mirrors web footer: fascia + electric top edge) */
.tabbar-edge {
  position: fixed;
  left: 0;
  right: 0;
  bottom: calc(50px + env(safe-area-inset-bottom));
  height: 4rpx;
  background: var(--electric, #00ff85);
  z-index: 2;
  pointer-events: none;
}
