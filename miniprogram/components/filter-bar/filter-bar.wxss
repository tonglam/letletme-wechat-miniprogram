.filter-bar {
  display: flex;
  align-items: center;
  gap: 16rpx;
}

/* Layout-only; visuals come from the global .input primitive */
.filter-input {
  flex: 1;
}

.filter-button {
  flex-shrink: 0;
  height: 80rpx;
  margin: 0;
  padding: 0 22rpx;
  border-radius: 12rpx;
  background: var(--card);
  color: var(--ink);
  border: 1rpx solid var(--line);
  font-size: 24rpx;
  font-weight: 600;
  line-height: 78rpx;
  box-sizing: border-box;
}

.filter-button::after {
  border: 0;
}

.filter-button.primary {
  background: var(--cta);
  color: var(--cta-ink);
  border-color: transparent;
  font-weight: 700;
}
