/** Route entry; market loading and view-model orchestration lives in controller. */
export {
  buildLineup,
  buildPulseView,
  resolveTeamSearchId
} from "./price.controller";
import "./price.controller";
