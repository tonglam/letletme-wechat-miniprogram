/** Route entry; lifecycle/request orchestration is kept in the controller. */
export {
  HISTORY_PAGE_SIZE,
  TRANSFER_PAGE_SIZE,
  buildChipInventory,
  buildTransferView,
  phaseBannerFromSnapshot,
  retainTransferRowsAfterFailure
} from "./team.controller";
import "./team.controller";
