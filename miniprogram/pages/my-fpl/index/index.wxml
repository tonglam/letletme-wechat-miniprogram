<view class="page">
  <view class="section">
    <my-fpl-phase-card
      phase="{{phase}}"
      principalState="{{principalState}}"
      deadlineText="{{deadlineText}}"
      teamBrief="{{teamBrief}}"
      offline="{{offline}}"
      bind:primary="onPhasePrimary"
      bind:secondary="onPhaseSecondary"
    />
  </view>

  <view wx:if="{{error}}" class="section">
    <view class="inline-error">
      <text>{{error}}</text>
      <view class="inline-error-retry" bindtap="onRetry">重试</view>
    </view>
  </view>

  <view wx:if="{{principalState !== 'NO_FOLLOW'}}" class="section">
    <view class="module" bindtap="onOpenLeagues">
      <view>
        <view class="module-title">我的联赛</view>
        <view class="module-subtitle">{{leaguesLoaded ? (leagueCount > 0 ? leagueCount + ' 个官方联赛' : '暂无官方联赛') : '加载中'}}</view>
      </view>
      <text class="module-arrow">查看</text>
    </view>
  </view>

  <bottomNavBar active="myFpl" />
</view>
