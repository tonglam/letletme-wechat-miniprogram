/**
 * Mock data switch for UI validation.
 * Set to true to use mock data instead of live backend.
 * Set to false (or delete this file + mocks/) to restore normal operation.
 */
export const MOCK_ENABLED = true;
